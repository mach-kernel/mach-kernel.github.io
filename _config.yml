title: David Stancu
email: dstancu@nyu.edu
description: > # this means to ignore newlines until "baseurl:"
  Polyglot software architect and car enthusiast living in New York City.
baseurl: "" # the subpath of your site, e.g. /blog
url: "" # the base hostname & protocol for your site, e.g. http://example.com

twitter_username: mach_kernel
github_username:  mach-kernel
linkedin_username: david-stancu-84557335
instagram_username: pragmadev

# Build settings
markdown: kramdown
plugins:
  - jekyll-feed
  # - jekyll-responsive-image
assets:
  compress:
    js: true
  sources:
    - _assets/css
    - _assets/javascripts
exclude:
  - node_modules

responsive_image:
  # [Required]
  # Path to the image template.
  template: _includes/responsive-image.html

  # [Optional, Default: 85]
  # Quality to use when resizing images.
  default_quality: 90

  # [Optional, Default: []]
  # An array of resize configuration objects. Each object must contain at least
  # a `width` value.
  sizes:
    - width: 480  # [Required] How wide the resized image will be.
      quality: 80 # [Optional] Overrides default_quality for this size.
    - width: 800
    - width: 1400
      quality: 90

  # [Optional, Default: false]
  # Rotate resized images depending on their EXIF rotation attribute. Useful for
  # working with JPGs directly from digital cameras and smartphones
  auto_rotate: false

  # [Optional, Default: assets]
  # The base directory where assets are stored. This is used to determine the
  # `dirname` value in `output_path_format` below.
  base_path: _assets

  # [Optional, Default: _assets/resized/%{filename}-%{width}x%{height}.%{extension}]
  # The template used when generating filenames for resized images. Must be a
  # relative path.
  #
  # Parameters available are:
  #   %{dirname}     Directory of the file relative to `base_path` (_assets/sub/dir/some-file.jpg => sub/dir)
  #   %{basename}    Basename of the file (_assets/some-file.jpg => some-file.jpg)
  #   %{filename}    Basename without the extension (_assets/some-file.jpg => some-file)
  #   %{extension}   Extension of the file (_assets/some-file.jpg => jpg)
  #   %{width}       Width of the resized image
  #   %{height}      Height of the resized image
  #
  output_path_format: _assets/resized/%{width}/%{basename}

  # [Optional, Default: true]
  # Whether or not to save the generated _assets into the source folder.
  save_to_source: false

  # [Optional, Default: []]
  # By default, only images referenced by the responsive_image and responsive_image_block
  # tags are resized. Here you can set a list of paths or path globs to resize other
  # images. This is useful for resizing images which will be referenced from stylesheets.
  extra_images:
    - _assets/foo/bar.png
    - _assets/bgs/*.png
    - _assets/avatars/*.{jpeg,jpg}